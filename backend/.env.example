# Optional API keys — see scripts/SETUP_KEYS.md for signup links.
# Without these, the system still works but skips fundamentals + earnings.

FMP_API_KEY=
FINNHUB_API_KEY=

# iaedu.pt agent (Ideas → Todos, Investment Signals, etc.)
AI_API_KEY=

# CORS: separar URLs por vírgula. Vazio = só localhost.
ALLOWED_ORIGINS=

DATABASE_URL=sqlite:///./cristopher.db
UPLOADS_DIR=uploads

# 1 = arranca o app_tracker do macOS junto com o backend (só funciona em macOS)
TRACKER_AUTOSTART=1
